<div class='form-main'>
    <div class="form-submain">
        <h1 class="ln">Buscar Descuento</h1>
        <form class="div-form-main" onsubmit="event.preventDefault(); getDescuento();" method="post">
            Código: <input type="text" class="input" name="hash" required><br>
            Descuento: <h2 id="descuento"></h2><br>
            <br>
            <input type="submit" class="login" value="Buscar">
        </form>
    </div>
</div>
<script>
    function getDescuento() {
    const hashInput = document.querySelector('input[name="hash"]');
    const descuentoElement = document.getElementById('descuento');

    fetch('/hash/getDesc', {
        method: 'POST',
        headers: {
        'Content-Type': 'application/json'
        },
        body: JSON.stringify({ hash: hashInput.value })
    })
    .then(response => response.json())
    .then(data => {
        if (data.descuento) {
        descuentoElement.textContent = data.descuento;
        } else {
        descuentoElement.textContent = 'Descuento no encontrado.';
        }
    })
    .catch(error => {
        console.error('Error:', error);
        descuentoElement.textContent = 'Error al obtener el descuento.';
    });
    }
</script>

<style>
    .form-main {
        display: flex;
        height: calc(100vh - 100px);
        align-items: center;
        justify-content: center;
    }
    
    .form-submain {
        background-color: #1a202c;
        max-width: 25rem;
        width: 100%;
        padding: 2.5rem 3rem; /* Ajuste del padding, aumentando el valor de la derecha */
        border-radius: 1.1rem;
    }

    .input {
        width: 100%;
        background-color: #4a5568;
        color: #fff;
        padding: 0.5rem 0.11rem;
        border-radius: 0.375rem;
        margin-top: 0.5rem;
        margin-bottom: 0.5rem;
    }

    .login {
        background-color: #109e52;
        padding: 0.5rem;
        border-radius: 0.375rem;
        margin-top: 0.5rem;
        margin-bottom: 0.5rem;
        margin: auto;
        color: white;
    }
    .ln {
    font-size: 1.5rem; /* Equivalente a text-2xl en Tailwind CSS */
    font-weight: bold; /* Equivalente a font-bold en Tailwind CSS */
    margin-top: 0.5rem;
    margin-bottom: 0.5rem;
    }

</style>
